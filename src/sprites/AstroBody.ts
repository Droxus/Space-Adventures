import { Graphic } from "../core/Graphic";

/**
 * An instance of this class represents either a planet, or a start,
 * which a {@link Player} uses to spawn, loot, complete quests on.
 */
export class AstroBody extends Graphic {
    /**
     * @todo implement
     */
}